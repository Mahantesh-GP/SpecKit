Before /speckit.tasks, perform one final compatibility correction pass.

1. FR-020:
Do not introduce new string-to-number parsing unless explicitly approved.
Preserve the approved extractor behavior:
- numeric Excel int/float values -> numeric value
- string, datetime, error, or other non-numeric metric values -> null
- no percentage scaling
If this is intentionally changing, surface it for approval instead of silently changing compatibility behavior.

2. FR-010:
Do not state "positive integer" unless this is an explicitly approved new rule.
Align with the approved row-ID interpretation from the reference extractor, including numeric values such as 1.0 being accepted and non-numeric label rows being skipped.

3. Header scan:
Reconcile the source-document inconsistency between "first 10 rows" and the extractor implementation reference stating the first 11 rows.
Prefer the implementation reference unless there is stronger approved evidence.
Document the exact final rule.

4. Duplicate DR identity:
Ensure the rule rejects two submitted workbooks that resolve to the same DR identity using the approved filename-derived DR parsing rule.
Do not reduce this to exact duplicate filename detection only.

Update affected artifacts and re-evaluate readiness.md.
Do not generate tasks yet.


------------------------


Correct FR-010 to match the approved extractor exactly.

Do not add a "positive integer" or "lossless integer" rule.

Approved behavior:
- If the row-number cell is null/blank/falsy, treat it as an empty row for
  end-of-data detection; it is not extracted as a data row.
- Otherwise attempt the same integer conversion used by the reference extractor.
- If integer conversion fails, silently treat the row as a non-data/label row.
- If conversion succeeds, use the converted integer row number and extract the row.
- Do not separately reject fractional numeric values unless the approved extractor
  actually does so.
- Do not claim numeric 0 is a valid data-row ID because the reference empty-row
  check treats a falsy row-number value as empty.

Update spec.md and data-model.md consistently, then re-evaluate readiness.md.
Do not generate tasks yet.



-------------------------

Resolve the /speckit.analyze findings before implementation.

HIGH:

H1:
Org, Unit, Team, PM and Members are in scope for Feature 001.
Add them to NormalizedRolloutRow in data-model.md and update T012 accordingly.
Do not remove them from FR-011.

H2:
Keep IImportLockService as the application abstraction.
Add an Infrastructure implementation task for ImportLockService using the
planned EF Core optimistic-concurrency/ImportLock mechanism.
Update T030 SubmitImportBatch to consume IImportLockService rather than
implementing EF Core locking directly.

MEDIUM:

M1:
Update T020 to use/document the manual EF migration application step defined
by quickstart.md. Remove the startup-or-manual ambiguity.

M2:
Make T034 the owner of complete FR-028 unreadable-workbook handling.
Remove wording from T030 that implies FR-028 is already fully implemented in US1.

M3:
Make T028 the single implementation owner for FR-031 unrecognized-column
detection and diagnostic recording.
T049 should only verify the end-to-end behavior.
Remove conflicting ownership from T035.

M4:
Update T016 to explicitly configure no cascade delete for the retained import
audit chain ImportBatch -> WorkbookSubmission -> ImportDiagnostic.

M5:
Update T033 to sanitize the client-supplied filename before storing or passing it
to application logic. Remove path separators/control characters and treat the
filename only as display/source metadata.

LOW:

L1:
Remove duplicate lock-release implementation detail from T030 and keep T046 as
the explicit lock-release hardening/verification task.

L2:
Use distinct diagnostic codes for:
- duplicate row number within a sheet
- duplicate row number across DR workbooks

Update tasks.md and any affected design artifact.
Then rerun /speckit.analyze.

Do not implement code yet.