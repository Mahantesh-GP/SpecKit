

Run /speckit.clarify and paste:
A real production DR workbook has revealed an additional source-schema rule that Feature 001 must support.

The workbook contains three repeated header cells named "ETA":
- ETA immediately following the Phase 1 status column belongs to Phase1Eta
- ETA immediately following the Phase 2 status column belongs to Phase2Eta
- ETA immediately following the Phase 3 status column belongs to Phase3Eta

The current implementation appears to resolve headers using header text as a unique dictionary key, causing repeated "ETA" headers to overwrite each other and only the final ETA to be retained.

Do NOT require the business Excel workbook to rename these columns.

Clarify and update the Feature 001 specification so that:
1. Duplicate header names such as ETA are resolved using their positional/phase context rather than assuming header names are globally unique.
2. The first ETA associated with Phase 1 maps to Phase1Eta.
3. The ETA associated with Phase 2 maps to Phase2Eta.
4. The ETA associated with Phase 3 maps to Phase3Eta.
5. All three values must be independently retained.
6. Existing date normalization/null handling rules continue to apply.
7. Existing supported canonical/header aliases must remain supported.
8. Add an acceptance scenario proving that three different ETA values from one row are imported into the corresponding Phase1Eta, Phase2Eta and Phase3Eta fields.

Update specification artifacts only at this stage. Do not modify implementation code yet.


After clarify finishes, run /speckit.plan
Update the Feature 001 implementation plan and supporting artifacts for the newly clarified repeated-ETA header rule.

The HeaderResolver must support duplicate physical header names using column position and phase context instead of relying solely on a dictionary keyed by header text.

Preserve current architecture and existing behavior. Do not require any changes to the source Excel workbook.

Update relevant research, data-model, contracts and quickstart/testing guidance where applicable.


Run /speckit.tasks with:

Generate only the delta implementation and test tasks required for the repeated ETA header clarification.

Preserve all previously completed Feature 001 task completion states. Do not reset completed tasks.

Include:
- contextual/positional duplicate-header resolution
- Phase1Eta/Phase2Eta/Phase3Eta mapping
- regression/compatibility test using repeated ETA headers
- verification that existing header aliases still work