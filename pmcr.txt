Revise tasks.md to keep Phase 1 aligned with the agreed framework/reference implementation scope. Do not implement a real DocNav HTTP client or make any outbound DocNav network call in this feature. Remove T008 as a real HttpClient-based DocNavRequestService implementation and remove any downstream task dependencies that require the real service. The implementation must use IDocNavRequestService as the abstraction and DocNavRequestStubService as the locally executable implementation. IDocNavAccessTokenProvider and IDocNavDocumentSource should remain extension contracts for a future consuming-team production implementation; do not implement Managed Identity/token acquisition, real blob/file retrieval, multipart DocNav submission, or real Development-environment calls. Preserve the documented DocNav API contract in the models/interfaces where useful so a consuming team can later replace the stub with a real IDocNavRequestService without changing DocNavRequestActivity. Update MVP wording accordingly: Phase 1 proves WorkflowActivityInput → DocNavRequestActivity → IDocNavRequestService → Stub → ActivityExecutionResult with zero network calls. Keep optional real DocNav integration explicitly out of scope for this feature.